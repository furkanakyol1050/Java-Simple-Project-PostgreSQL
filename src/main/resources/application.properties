spring.application.name=sql_demo

## PostgreSQL || postgres => DB adi
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
## Scheme name
spring.jpa.properties.hibernate.default_schema=student
spring.datasource.username=postgres
spring.datasource.password=123qwe

## create and drop table, good for testing, production set to none or comment it
## create = komple siler || update = silmez üstüne ekler
## !!! Dogru scheme de olusabilmesi için önce create ile tablo olusturulmali
## yoksa public e olusturuyor. Önce create e al sonra update e çek.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
